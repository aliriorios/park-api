# Test Database Config
spring:
  profile:
    active: test
  datasource:
    url: jdbc:mysql://localhost:3307/park-api-db-test?useSSL=false&allowPublicKeyRetrieval=true
    username: alirio-dev
    password: dev
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      show-sql: true
      properties:
        hibernate:
          format_sql: true
          dialect: org.hibernate.dialect.MySQL8Dialect